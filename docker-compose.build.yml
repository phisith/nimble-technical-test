version: '3.8'

services:
  frontend:
    build: ./frontend
    command: npm install --force

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    command: npm install

